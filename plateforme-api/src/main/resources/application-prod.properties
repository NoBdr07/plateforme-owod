# General
spring.application.name=plateforme-api
server.port=5000
app.frontend.url=https://owod.aipda-design.org

# Variables d'environnement
owod.plateforme-api.jwtSecret=${JWT_SECRET}
owod.plateforme-api.jwtExpirationMs=${JWT_DURATION}

# Configuration MongoDB
spring.data.mongodb.uri=${MONGO_URI}

# Encodage caracteres
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Gestion cache
spring.web.resources.cache.cachecontrol.max-age=0
spring.web.resources.cache.cachecontrol.no-cache=true
spring.web.resources.cache.cachecontrol.must-revalidate=true

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

mail.contact=info@aipda-design.org

# Log
logging.level.org.mongodb.driver=DEBUG

# Cookie
cookie.secure=true
cookie.httpOnly=true
cookie.sameSite=true

# Aws S3
aws.s3.region=eu-west-3
aws.s3.access-key=${AWS_ACCESS}
aws.s3.secret-key=${AWS_SECRET}
aws.s3.bucket-name=owod-images


